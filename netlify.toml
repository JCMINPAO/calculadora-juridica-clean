[build]
  publish = "."
  command = "echo 'No build command needed'"

# Configuración para omitir escaneo de secretos
[build.environment]
  SECRETS_SCAN_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200